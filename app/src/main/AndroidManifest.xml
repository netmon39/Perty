<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.netipol.perty">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.AccountActivity" />
        <activity android:name=".Login.SelectPrefActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity
            android:name=".GridViewAdapter"
            android:label="@string/title_activity_grid_view_adapter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GridItemView"
            android:label="@string/title_activity_grid_item_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SelectedItemsActivity"
            android:label="@string/title_activity_selected_items"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login.Home.PostActivity" />
        <activity android:name=".Login.Home.BaseActivity"></activity>
    </application>

</manifest>